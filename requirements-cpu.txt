# Exact research environment, CPU only. Torch 2.10.0 CPU build. For an AMD
# GPU use requirements-amd.txt; for NVIDIA, use requirements.txt.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu
torchvision==0.25.0+cpu

numpy==2.4.2
pillow==12.1.1
matplotlib==3.10.8
scikit-learn==1.8.0
requests==2.32.5

# Georeferencing and vector output (predict --geojson, evaluate)
affine>=2.4
rasterio>=1.3
shapely>=2.0
geopandas>=0.14

# Serving (labelnet serve)
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9

# Development and testing
pytest>=8.0
