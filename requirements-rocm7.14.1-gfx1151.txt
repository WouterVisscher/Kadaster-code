# Exact research environment for AMD GPUs (ROCm). Torch 2.10.0 built with
# ROCm 7.1; the wheel bundles the HIP runtime, so only a working AMD GPU
# driver is required (verify with `rocm-smi`). For a CPU install, use
# requirements-cpu.txt; for NVIDIA, use requirements.txt.
--extra-index-url https://repo.amd.com/rocm/whl-multi-arch/
torch[device-gfx1151]==2.12.0+rocm7.14.1
torchvision[device-gfx1151]==0.27.0+rocm7.14.1

numpy==2.4.2
pillow==12.1.1
matplotlib==3.10.8
scikit-learn==1.8.0
requests==2.32.5

# Georeferencing and vector output (predict --geojson, evaluate)
affine>=2.4
rasterio>=1.3
shapely>=2.0
geopandas>=0.14

# Serving (labelnet serve)
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9

# Development and testing
pytest>=8.0
