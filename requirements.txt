# Core dependencies.
# Install torch/torchvision first via the method for your platform (see README.md);
# pip will then treat them as satisfied and skip them here.
numpy==2.4.2
pillow==12.1.1
matplotlib==3.10.8
scikit-learn==1.8.0
requests==2.32.3
torch
torchvision
