# Exact research environment (CUDA 12.8). For a CPU install, use
# `pip install -e ".[dev]"` after installing the CPU torch wheel, see README.
--find-links https://download.pytorch.org/whl/cu128
torch==2.10.0
torchvision==0.25.0

numpy==2.4.2
pillow==12.1.1
matplotlib==3.10.8
scikit-learn==1.8.0
requests==2.32.5

# Georeferencing and vector output (predict --geojson, evaluate)
affine>=2.4
rasterio>=1.3
shapely>=2.0
geopandas>=0.14

# Serving (labelnet serve)
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9

# Development and testing
pytest>=8.0
